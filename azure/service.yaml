apiVersion: v1
kind: Service
metadata:
  name: easy-shopping-frontend
spec:
  type: LoadBalancer
  selector:
    app: easy-shopping-frontend
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      name: http